# .cursor

Cursor agent surface for packedbox. Canonical skills live under `tools/harness/skills/`; this directory provides discovery symlinks (same pattern as [devprofile](https://github.com/p10ns11y/devprofile) `.cursor/skills` → `.agents/skills`).

| Path | Target |
|------|--------|
| `skills/packedbox` | `../../tools/harness/skills/packedbox` |

Pinned versions: [`skills-lock.json`](../skills-lock.json) at repo root.
