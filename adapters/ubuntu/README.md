# adapters/ubuntu/

Ubuntu adapter — primary **Phase 1 CI target** for PATH + recover + fix-path.

**Phase 1 deliverables:**

- apt package list for shell test dependencies
- Hook from `installers/install.sh` when `/etc/os-release` ID is `ubuntu`
