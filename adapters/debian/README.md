# adapters/debian/

Debian adapter — apt-based installs with Triexe-oriented package name mapping.

**Phase 2:** Replace stub with real `adapter_install` / `adapter_packages` functions.

Debian and Ubuntu share apt but may diverge on package names; keep separate adapters.
