# adapters/arch

Arch Linux (+ Omarchy) adapter.

**Upstream:** [arch-machine](https://github.com/p10ns11y/arch-machine) `sentinel` — `install.sh`, `modules/`, `maintenance/`.

**Phase 2+:** pacman/paru hooks, Omarchy detection (see shellyxz `environments/omarchy/`).
