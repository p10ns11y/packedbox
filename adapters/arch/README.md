# adapters/arch/

Arch Linux adapter — pacman/paru package maps and Omarchy hooks.

**Phase 2–3:** Real package lists from arch-machine `config/tools.yaml` and module install scripts.

**Stub until then:** `install.sh` detects `/etc/arch-release` and delegates here.
