#include <stdio.h>
#include "packedbox.h"

int main(int argc, char **argv) {
    (void)argc;
    (void)argv;
    printf("packedbox %s (Phase 0 scaffold — elomaxz core in Phase 3)\n", PACKEDBOX_VERSION);
    return 0;
}
