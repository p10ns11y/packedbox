# packs/terminal/

v1 terminal pack: **Ghostty + tmux + Neovim**.

Configs pulled selectively from arch-machine `modules/productivity/eye-comfort/` (Phase 3).

| Subdir | Contents (planned) |
|--------|-------------------|
| `ghostty/` | Theme fragments from eye-comfort |
| `tmux/` | Verification cockpit layouts |
| `nvim/` | Theme hot-reload lua |
| `hooks/` | reload-nvim-tmux on theme change |

Install via `./installers/install.sh --pack terminal` (Phase 3).
